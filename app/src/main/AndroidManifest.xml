<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.miftarom.sablonapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SablonApp">
        <activity android:name=".users.HomUser"></activity>
        <activity android:name=".pelanggan.Etalase" />
        <activity android:name=".admin.HomAdmin2" />
        <activity android:name=".users.Regis" />
        <activity android:name=".MainActivity" />
        <activity android:name=".users.Login">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>